import { DragObjectWithType } from 'react-dnd';
import ID from './types/ID';
import ItemData from './types/ItemData';
import { ItemProps } from './Item';
export declare type SortlyProps<D = {
    id: ID;
}> = {
    type?: DragObjectWithType['type'] | (() => DragObjectWithType['type']);
    items: ItemData<D>[];
    threshold?: number;
    maxDepth?: number;
    horizontal?: boolean;
    onChange: (items: ItemData<D>[]) => void;
    children: ItemProps<D>['children'];
};
declare function Sortly<D = {
    id: ID;
}>(props: SortlyProps<D>): JSX.Element;
export default Sortly;
